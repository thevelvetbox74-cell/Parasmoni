rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Completely permissive rules for live catalog and showroom operations across all collections and subcollections
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
